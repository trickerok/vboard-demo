rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /rooms/{roomId}/objects/{objectId} {
      allow read, write: if true; // Allow all users to collaborate in rooms
    }
  }
}
